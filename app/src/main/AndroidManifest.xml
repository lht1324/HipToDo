<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.overeasy.simpletodo">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- SplashActivity
        <activity android:name=".ui.SplashActivity"
            android:theme="@style/SplashTheme">
        </activity> -->

        <!-- IntroActivity-->
        <activity android:name="com.overeasy.simpletodo.view.IntroActivity">
        </activity>

        <!-- MainActivity-->
        <activity android:name="com.overeasy.simpletodo.view.MainActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="io.sentry.dsn"
            android:value="https://bdcf156ab2424e7dbc09f3869129ea78@o430908.ingest.sentry.io/5529964" />

    </application>

</manifest>